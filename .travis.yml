language: groovy

script:
  - echo 'skip'

deploy:
  - provider: script
    skip_cleanup: true
    script: echo 'deploying feature!'
    on:
      branch:
        - /feature\/.+/
  - provider: script
    skip_cleanup: true
    script: echo 'deploying release!'
    on:
      branch:
        - /release\/.+/
